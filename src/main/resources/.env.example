# 데이터베이스 연결 설정
DB_TYPE=mysql
DB_HOST=prod-db-hostname
DB_PORT=3306
DB_NAME=prod_database
DB_PARAMS=useSSL=true&requireSSL=true   # 주의: Server TimeZone 절대 미포함
DB_USERNAME=prod_user
DB_PASSWORD=secure_password
DB_DRIVER=com.mysql.cj.jdbc.Driver

# JPA/Hibernate 설정
DB_DDL_AUTO=none                    # 운영에서는 스키마 변경 금지
BATCH_FETCH_SIZE=100                # 운영 환경에 맞게 최적화된 값
FORMAT_SQL=false                    # 불필요한 로깅 방지
DEFER_INIT=false

# SQL 초기화 설정
SQL_DATA_LOCATION=                  # 운영 환경에서 초기 데이터 적용 안함
SQL_INIT_MODE=never                 # 운영 환경에서 초기화 금지

# 로깅 설정
LOG_LEVEL_SPRING=WARN              # 필요한 경고만 로깅
LOG_LEVEL_SQL=ERROR                # 에러만 로깅

# p6spy (비활성화)
P6_SPY=false                       # 성능 영향 방지

# storage
STORAGE_BASE_URL=https://example.greenwinit.store   # cdn 주소
STORAGE_BUCKET=bucket                               # s3 버켓 이름
DEFAULT_PROFILE_IMAGE_URL=
SWAGGER_PATH=

# mail
RECEIVER_EMAIL_ADDRESS=example@gmail.com    #test mail 주소
RECEIVER_EMAIL_PASSWORD=zydktoyvverebcxd

MAX_FILE_SIZE=
MAX_REQUEST_SIZE=

# oauth
OAUTH2_GOOGLE_CLIENT_NAME=google
OAUTH2_GOOGLE_CLIENT_ID=clientId
OAUTH2_GOOGLE_CLIENT_SECRET=secret
OAUTH2_GOOGLE_REDIRECT_URI=redirect_uri
OAUTH2_GOOGLE_GRANT_TYPE=grant_type
OAUTH2_GOOGLE_SCOPE=scope

OAUTH2_NAVER_CLIENT_NAME=naver
OAUTH2_NAVER_CLIENT_ID=D0rAmUvq0gBr3QT8J834
OAUTH2_NAVER_CLIENT_SECRET=secret
OAUTH2_NAVER_REDIRECT_URI=redirect_uri
OAUTH2_NAVER_GRANT_TYPE=grant_type
OAUTH2_NAVER_SCOPE=scope

OAUTH2_KAKAO_CLIENT_NAME=kakao
OAUTH2_KAKAO_CLIENT_ID=clientId
OAUTH2_KAKAO_CLIENT_SECRET=secret
OAUTH2_KAKAO_REDIRECT_URI=redirect_uri
OAUTH2_KAKAO_GRANT_TYPE=grant_type
OAUTH2_KAKAO_SCOPE=scope

# AWS
AWS_ACCESS_KEY=access
AWS_SECRET_KEY=secret
AWS_STATIC_REGION=

# JWT
JWT_SECRET=secret
JWT_ACCESS_EXPIRATION=10000
JWT_REFRESH_EXPIRATION=10000
JWT_TEMP_EXPIRATION=10000

# service domain
FRONTEND_URL=front
BACKEND_URL=backend